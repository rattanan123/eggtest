# รายงานวิเคราะห์โปรเจคตู้ฟักไข่อัจฉริยะ

> วิเคราะห์โดย Claude — อ้างอิงโค้ดในไฟล์ `DHT22Egg.ino` (485 บรรทัด) และ `index.html` (1,358 บรรทัด)

## 1. สรุปภาพรวม

โปรเจคนี้เป็นระบบตู้ฟักไข่อัจฉริยะ ทำงานบนบอร์ด ESP32 ควบคุมการเปิด-ปิดอุปกรณ์ผ่านรีเลย์ 5 ตัวและเซอร์โวสำหรับพลิกไข่ อ่านอุณหภูมิและความชื้นจาก SHT45 เชื่อม Firebase Realtime Database แสดงผลผ่าน Web Dashboard และแจ้งเตือนผ่าน LINE

### องค์ประกอบหลัก

- **เฟิร์มแวร์** `DHT22Egg.ino` — ESP32 + SHT45 + รีเลย์ 5 ตัว + เซอร์โว SG90
- **หน้าเว็บ** `index.html` — JavaScript ล้วน + Firebase Web SDK + Chart.js
- **หลังบ้าน** — Firebase Realtime Database (asia-southeast1) + LINE Messaging API

### ฟีเจอร์ที่มีอยู่แล้ว

- ควบคุมอุณหภูมิด้วยฮีตเตอร์แบบเปิด-ปิด รอบค่ากลางระหว่าง `tempMin` และ `tempMax`
- ควบคุมความชื้นด้วยพ่นหมอกและพัดลม แบบ hysteresis
- พลิกไข่ด้วยเซอร์โว 135° → 90° → 45° → 90° วน ตั้งเวลาค้าง 0.1–24 ชม. ผ่าน `servoHoldHours`
- ตั้งระยะเวลาฟัก วัน/ชม./นาที พร้อมตัวนับถอยหลัง
- Dashboard real-time, กราฟ 24 ชม., log ราย 1 ชม., log การเปิด-ปิดระบบ
- สลับธีม light/dark
- แจ้งเตือน LINE: ระบบเริ่ม/หยุด, อุณหภูมิ/ความชื้นเกิน, sensor พัง, เหลือ 1 วัน/30 นาที/ครบกำหนด
- จัดการ sensor failure (ปิดรีเลย์ทุกตัวเมื่ออ่านพลาด 5 ครั้งติด)
- FreeRTOS task แยก servo และ LINE
- NTP time sync

## 2. สถาปัตยกรรมระบบ

| ชั้น | ส่วนประกอบ | หน้าที่ |
|---|---|---|
| ฮาร์ดแวร์ | ESP32 + SHT45 + รีเลย์ 5 + เซอร์โว SG90 | อ่านค่า สั่งงาน พลิกไข่ |
| เฟิร์มแวร์ | `DHT22Egg.ino` (Arduino C++) | Control logic, networking |
| ฐานข้อมูล | Firebase RTDB | `/control`, `/current`, `/logs`, `/systemLogs` |
| แจ้งเตือน | LINE Messaging API | Push ไปยัง user |
| หน้าเว็บ | `index.html` + Chart.js | Dashboard + ตั้งค่า + กราฟ |

### การไหลของข้อมูล

ESP32 อ่าน SHT45 ทุก 3 วินาที → คำนวณ → เขียน `/incubator/current` ทุก 5 วินาที → Dashboard subscribe ด้วย `onValue()` user ปรับค่า → เขียน `/incubator/control` → ESP32 อ่านทุก 3 วินาที

## 3. จุดที่ควรปรับปรุง

### 3.1 ความปลอดภัย (สำคัญที่สุด)

ข้อมูลลับฝังในโค้ดและเปิดเผยบน GitHub สาธารณะ:

- `WIFI_PASSWORD` — เปิดเผยรหัส WiFi
- `USER_PASSWORD` ของ Firebase Auth — ใครก็เข้าถึงและแก้ฐานข้อมูลได้
- `LINE_TOKEN` — ส่ง LINE แทนเจ้าของได้
- `API_KEY` ของ Firebase — แม้ไม่ลับโดยตรง แต่ต้องคู่กับ DB Rules ที่รัดกุม

**แนวทางแก้ไขเร่งด่วน:**

1. เปลี่ยนรหัสผ่านทุกตัวทันที (WiFi, ผู้ใช้ Firebase, LINE channel)
2. ลบประวัติ commit เก่าออกจาก git history (ใช้ `git filter-repo` หรือ BFG Repo-Cleaner)
3. ย้ายข้อมูลลับออกจากโค้ด ใส่ใน `secrets.h` ที่ `.gitignore` หรือ NVS (`Preferences`)
4. ใช้ `WiFiManager` ตั้งค่าผ่าน captive portal ครั้งแรก
5. ตั้ง Firebase Database Rules ให้อ่าน-เขียนได้เฉพาะ user ที่ login

### 3.2 การควบคุม

- ใช้ Bang-bang control ทำให้อุณหภูมิแกว่ง ควรเปลี่ยนเป็น PID + PWM
- ไม่มี Watchdog timer
- ไม่รองรับ OTA update
- ไม่มี local fallback (เน็ตดับ → สั่งงานไม่ได้)

### 3.3 การใช้งานจริงของตู้ฟักไข่

- ตั้งค่าเดียวตลอดรอบ แต่จริง ๆ ระยะ Lockdown 3 วันสุดท้ายต้องลดอุณหภูมิ เพิ่มความชื้น และหยุดพลิก
- ไม่มี Profile สำหรับสัตว์ปีกแต่ละชนิด
- ไม่บันทึกจำนวนไข่และอัตราฟัก

### 3.4 หน้าเว็บ Dashboard

- กราฟแสดงแค่ 24 ชม. ดูทั้งรอบฟัก 21 วันไม่ได้
- ไม่มี Export CSV/Excel
- ไม่ใช่ PWA
- ไม่มีระบบ login (ใครรู้ URL ก็คุมตู้ได้)

## 4. ตารางลำดับฟีเจอร์ใหม่

| # | ฟีเจอร์ | ผลที่ได้ | ความยาก |
|---|---|---|---|
| 1 | เพิ่มความปลอดภัย + WiFiManager | สูงมาก | ต่ำ |
| 2 | โปรไฟล์การฟักแบบหลายช่วง | สูงมาก | ปานกลาง |
| 3 | หยุดพลิกไข่อัตโนมัติช่วง Lockdown | สูง | ต่ำ |
| 4 | PID + PWM heater | สูง | ปานกลาง |
| 5 | บันทึกจำนวนไข่และอัตราฟัก | สูง | ปานกลาง |
| 6 | กราฟครอบคลุมทั้งรอบ | ปานกลาง | ต่ำ |
| 7 | Export CSV/Excel | ปานกลาง | ต่ำ |
| 8 | แจ้งเตือนวันส่องไข่ + บันทึกรูป | ปานกลาง | ปานกลาง |
| 9 | OTA firmware update | สูง | ต่ำ |
| 10 | PWA + Push Notification (FCM) | สูง | ปานกลาง |
| 11 | เซ็นเซอร์ตัวสำรอง | สูง | ต่ำ |
| 12 | เซ็นเซอร์ระดับน้ำ + ฝาตู้ | ปานกลาง | ต่ำ |
| 13 | ระบบสำรองไฟ + ตรวจไฟดับ | สูง | ปานกลาง |
| 14 | กล้องในตู้ + Web stream | ปานกลาง | สูง |
| 15 | ตรวจจับความผิดปกติด้วย AI | ปานกลาง | สูง |

ดูรายละเอียดและแนวทาง implement แต่ละข้อใน [ROADMAP.md](./ROADMAP.md)

## 5. สรุปส่งท้าย

โปรเจคมี core ที่แข็งแรงแล้ว จุดที่ควรปรับด่วนคือเรื่อง security (credentials บน public repo) และ sensor redundancy (สำคัญต่อชีวิตของไข่) จากนั้นค่อยทำ Multi-stage Profile ซึ่งจะใช้ฟักได้ครบทุกชนิด ไม่ใช่แค่ไก่

**ฟีเจอร์ที่ทำได้เร็วและคุ้มค่าที่สุด 3 อันดับแรก:**

1. **เพิ่มความปลอดภัย** (1–2 วัน, impact สูงมาก)
2. **หยุดพลิกไข่ช่วง Lockdown** (1 วัน, impact สูง — hatch rate ดีขึ้นทันที)
3. **OTA update** (ครึ่งวัน, impact สูง — แก้โค้ดต่อจากนี้ง่ายขึ้นมาก)
